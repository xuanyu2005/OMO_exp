[project]
name = "deep-kernel-swebench-omo"
version = "0.1.0"
description = "Local SWE-bench Verified Mini evaluation helpers for OpenCode + OMO experiments."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "swebench>=4.0.0",
]

[tool.uv]
package = false
