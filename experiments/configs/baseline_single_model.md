# A. Single Model Baseline

该组不使用 Oh-My-OpenAgent 插件。对每个任务使用同一模型和同一基础 prompt，直接让模型完成任务。

## 建议模型

- `gpt5.4mini` 或作业指定的 GPT mini 模型。

## 运行记录

每个任务记录：

- model
- prompt
- output file
- elapsed seconds
- estimated cost
- verification result
- human quality score

